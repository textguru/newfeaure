# 단위테스트 케이스 작성 템플릿 — 지사별 시행종목 조회

> 본 템플릿은 요구사항 정의서 V1.0 (REQ-001~004), 백엔드 API 설계서, DB 테이블 설계서를 기준선으로 **단위테스트 케이스를 일관되게 생성**하기 위한 양식이다. 각 요구사항의 검증기준(Acceptance Criteria)별로 1개 이상의 테스트 케이스를 생성한다.

---

## 1. 문서 정보

| 항목 | 내용 |
|:---|:---|
| **문서명** | 단위테스트 케이스 정의서 |
| **버전** | 1.0 |
| **작성일** | YYYY-MM-DD |
| **작성자** | [이름/팀] |
| **검토자** | [이름/역할] |
| **대상 모듈** | 지사별 시행종목 조회 (REQ-001~004 / SCR-001) |
| **테스트 프레임워크** | JUnit 5, Mockito, Spring Boot Test (MockMvc), AssertJ, Apache POI |

---

## 2. 테스트 식별자 규칙

```
TC-<REQ>-<Layer>-<NNN>
  REQ   : REQ-001 ~ REQ-004
  Layer : CTL(Controller) | SVC(Service) | REPO(Repository) | EXCEL | E2E
  NNN   : 3자리 순번 (001부터)

예) TC-REQ-003-SVC-001, TC-REQ-004-EXCEL-002
```

---

## 3. 테스트 범위 매트릭스 (Coverage)

| 요구사항 | 검증기준 # | Controller | Service | Repository | Excel | 비고 |
|:---|:---:|:---:|:---:|:---:|:---:|:---|
| REQ-001 시험정보 선택 | 1 / 2 / 3·예외1 | ✔ | ✔ | ✔ | - | 지사별 필터링, 0건 처리 |
| REQ-002 조회조건 설정 | 1 / 2 / 3 | ✔ | - | - | - | 화면 동작 (E2E 권장) |
| REQ-003 시행종목 목록 조회 | 1 / 2 / 3·예외2 | ✔ | ✔ | ✔ | - | 정렬·페이징·필수조건 |
| REQ-004 엑셀 다운로드 | 1 / 2 / 3·예외1 | ✔ | ✔ | - | ✔ | 파일명·헤더행·바이너리 |

---

## 4. 테스트 케이스 정의 템플릿 (1건당 반복 작성)

```markdown
### TC-<REQ>-<LAYER>-<NNN> : <케이스명>

| 항목 | 내용 |
|:---|:---|
| **요구사항 매핑**  | REQ-XXX 규칙 N / 검증기준 N (또는 예외처리 N) |
| **우선순위**       | P0 / P1 / P2 |
| **테스트 유형**    | Positive / Negative / Boundary / Exception |
| **테스트 대상**    | <패키지.Class#method> 또는 <HTTP METHOD URL> |
| **테스트 계층**    | Controller / Service / Repository / Excel / E2E |
| **사전 조건**      | - 데이터: <시드 데이터 / Mock 설정><br>- 환경: <H2, @Transactional 등> |
| **입력 데이터**    | <파라미터, 요청 본문, 메서드 인자> |
| **테스트 절차**    | **Given** <초기 조건><br>**When** <행위><br>**Then** <결과 확인> |
| **기대 결과**      | - HTTP/Return: <상태/값><br>- 응답 본문: <JSON 필드/값><br>- 부수 효과: <DB 변경/예외 메시지> |
| **검증 방법**      | assertThat(...), verify(...), MockMvc.andExpect(...) 등 |
| **자동화 여부**    | Yes (`<TestClass#method>`) / No |
| **결과**           | Pass / Fail / Pending |
| **실행자/일자**    | <이름> / YYYY-MM-DD |
| **비고**           | <관련 결함 ID, 회귀 여부> |
```

---

## 5. 계층별 JUnit 코드 스켈레톤

### 5.1 Controller — MockMvc

```java
@WebMvcTest(controllers = ExamItemController.class)
class ExamItemControllerTest {

    @Autowired MockMvc mockMvc;
    @MockBean ExamItemService examItemService;
    @MockBean ExcelExportService excelExportService;

    // TC-REQ-003-CTL-001 : 필수 파라미터 모두 정상
    @Test
    @DisplayName("[REQ-003] 정상 조회 시 200 + 페이징 응답")
    void searchItems_ok() throws Exception {
        // given
        Page<ExamItemResponse> page = new PageImpl<>(List.of(/* ... */),
                PageRequest.of(0, 20), /* total */ 1);
        given(examItemService.searchExamItems(eq(3), eq("BR001"),
                eq(LocalDate.parse("2026-08-10")), any())).willReturn(page);

        // when & then
        mockMvc.perform(get("/api/v1/exams/items")
                        .param("examId", "3")
                        .param("branchCode", "BR001")
                        .param("examDate", "2026-08-10"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.success").value(true))
                .andExpect(jsonPath("$.data.pagination.pageSize").value(20));
    }

    // TC-REQ-003-CTL-002 : 필수 파라미터 누락 → 400 + 메시지
    // TC-REQ-004-CTL-001 : 엑셀 다운로드 헤더(Content-Disposition, MIME)
}
```

### 5.2 Service — Mockito

```java
@ExtendWith(MockitoExtension.class)
class ExamItemServiceTest {

    @Mock ExamItemRepository repository;
    @InjectMocks ExamItemService service;

    // TC-REQ-003-SVC-001 : 페이지 누적 순번(seq) 부여 검증
    @Test
    @DisplayName("[REQ-003 규칙3] 순번은 조회 결과 기준 1번부터 누적 부여된다")
    void seqContinuesAcrossPages() {
        // given
        Pageable p2 = PageRequest.of(1, 20);
        given(repository.searchExamItems(eq(3), eq("BR001"),
                eq(LocalDate.parse("2026-08-10")), eq(p2)))
                .willReturn(new PageImpl<>(List.of(new ExamItemResponse(/*...*/)),
                        p2, 25));

        // when
        Page<ExamItemResponse> result = service.searchExamItems(
                3, "BR001", LocalDate.parse("2026-08-10"), p2);

        // then
        assertThat(result.getContent().get(0).getSeq()).isEqualTo(21L);
    }

    // TC-REQ-003-SVC-002 : 필수 조건 누락 시 BusinessException("조회 조건을 확인해 주세요.")
}
```

### 5.3 Repository — @DataJpaTest

```java
@DataJpaTest
@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
@Sql("/sql/test-data.sql")
class ExamItemRepositoryTest {

    @Autowired ExamItemRepository repository;

    // TC-REQ-003-REPO-001 : 정렬 규칙(itemCode ASC → part ASC)
    @Test
    @DisplayName("[REQ-003 규칙6] itemCode ASC, part ASC 순으로 정렬된다")
    void sortByItemCodeThenPart() {
        Page<ExamItemResponse> p = repository.searchExamItems(
                3, "BR001", LocalDate.parse("2026-08-10"), PageRequest.of(0, 20));
        assertThat(p.getContent())
                .extracting(ExamItemResponse::getItemCode,
                            ExamItemResponse::getPart)
                .isSortedAccordingTo(/* itemCode then part comparator */);
    }

    // TC-REQ-001-REPO-001 : findByBranchCodeWithItemCount() 의 시행종목 건수 계산 정확성
}
```

### 5.4 Excel — Apache POI 검증

```java
class ExcelExportServiceTest {

    // TC-REQ-004-EXCEL-001 : 헤더 행 8개 컬럼 검증
    @Test
    @DisplayName("[REQ-004 규칙4] 엑셀 1행은 컬럼 헤더로 구성된다")
    void headerRowContainsAllColumns() throws Exception {
        ExcelExportService svc = new ExcelExportService(repo, infoRepo, branchRepo);
        ExcelExportService.ExcelFile file = svc.exportExcel(
                3, "BR001", LocalDate.parse("2026-08-10"));

        try (Workbook wb = new XSSFWorkbook(new ByteArrayInputStream(file.content()))) {
            Row header = wb.getSheetAt(0).getRow(0);
            assertThat(header.getCell(0).getStringCellValue()).isEqualTo("순번");
            // ... 종목코드, 종목명, 선택분야, 선택분야명, 작업구분, 부, 시험일정공개여부
        }
    }

    // TC-REQ-004-EXCEL-002 : 파일명 형식 (지사별시행종목_[지사명]_[시험명]_[시험일자]_[일시].xlsx)
    // TC-REQ-004-EXCEL-003 : 데이터 0건 시 ResourceNotFoundException("다운로드할 데이터가 없습니다.")
}
```

---

## 6. 예시 작성 (REQ-003 검증기준 3 기반)

### TC-REQ-003-CTL-002 : 필수 조회 조건 누락 시 400 + 안내 메시지

| 항목 | 내용 |
|:---|:---|
| **요구사항 매핑** | REQ-003 규칙4 / 검증기준3 / 예외처리2 |
| **우선순위**     | P0 |
| **테스트 유형**  | Negative / Exception |
| **테스트 대상**  | `GET /api/v1/exams/items` (`ExamItemController#getExamItems`) |
| **테스트 계층**  | Controller (WebMvc) |
| **사전 조건**    | `ExamItemService` Mock 처리, 호출되어선 안 됨 |
| **입력 데이터**  | `branchCode=BR001` 만 전달 (examId, examDate 미전달) |
| **테스트 절차**  | **Given** examId·examDate 미설정<br>**When** `GET /api/v1/exams/items?branchCode=BR001`<br>**Then** 400 응답 + `{success:false, message:"조회 조건을 확인해 주세요."}` |
| **기대 결과**    | HTTP 400, JSON `success=false`, `message="조회 조건을 확인해 주세요."`, 서비스 미호출 |
| **검증 방법**    | `andExpect(status().isBadRequest())`, `jsonPath("$.message")`, `verifyNoInteractions(examItemService)` |
| **자동화**       | Yes (`ExamItemControllerTest#searchItems_missingRequired`) |
| **결과**         | Pending |
| **비고**         | GlobalExceptionHandler 의 `MissingServletRequestParameterException` 처리 검증 포함 |

---

## 7. 작성 가이드 (체크리스트)

- [ ] REQ-001~004 각 **검증기준 1건당 최소 1개** 테스트 케이스 생성
- [ ] **예외 처리(예외1·2) 각 1개 이상** 별도 케이스로 분리
- [ ] **경계값(Boundary)**: 페이지 1·마지막, 결과 0/1/20/21건, 페이지 크기 변경
- [ ] **계층 책임 분리**: Controller(요청·응답 매핑), Service(비즈니스 규칙), Repository(쿼리/정렬)
- [ ] Given/When/Then 한 절씩 한 줄로 명확히
- [ ] **하드코딩 금지**: 테스트 데이터는 `@Sql` 또는 빌더 팩토리로 관리
- [ ] **실패 메시지(`DisplayName`)에 REQ-ID 기입**으로 추적성 확보
- [ ] 회귀 위험 케이스는 `@Tag("regression")` 부여
- [ ] 커버리지 목표: Service ≥ 90%, Controller ≥ 85%, 전체 라인 ≥ 80%

---

## 8. 테스트 데이터 시드 규칙 (참고)

- 지사: BR001 ~ BR006 (DB 설계서 BRANCH_CODE 규칙 준수)
- 시험: 1=필기, 2=실기(시행종목 0건), 3=통합(시행종목 25건, 2페이지 케이스)
- 종목: C001 국어, E001 영어, H001 역사, M001 수학, S001 서술형 논술
- 시드 위치: `src/test/resources/sql/test-data.sql` (운영 `data.sql`과 분리)

---

*문서 종료*
